# CI Helpers

This folder is for CI fragments, matrices, and helper scripts that are easier to manage outside `.github/workflows/`.

Use `build_matrix.yml` as the source of truth for supported job groups when the CI pipeline expands.
